apiVersion: v1
kind: Service
metadata:
  name: http-server
  labels:
    app: http-server
spec:
  type: LoadBalancer
  selector:
    app: http-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 
